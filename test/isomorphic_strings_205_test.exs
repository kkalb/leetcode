defmodule IsomorphicStrings205Test do
  @moduledoc """
  Tests for isomorphic strings.
  """
  use ExUnit.Case
  alias IsomorphicStrings205

  test "greets the world" do
    assert IsomorphicStrings205.hello() == :world
  end
end
